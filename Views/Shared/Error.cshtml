@model ErrorViewModel
@{
    ViewData["Title"] = "Erro";
}

<h1 class="text-danger">Erro.</h1>
<h2 class="text-danger">Ocorreu um erro ao processar o seu pedido.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID do Pedido:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de Desenvolvimento</h3>
<p>
    Ativar o ambiente <strong>Development</strong> mostra mais detalhes sobre o erro.
</p>
<p>
    <strong>Não ative o modo Development em produção.</strong>
    Pode expor informações sensíveis a utilizadores finais.
    Para depuração local, defina a variável de ambiente <strong>ASPNETCORE_ENVIRONMENT</strong> como <strong>Development</strong> e reinicie a aplicação.
</p>
